# GitHub Actions Runner Environment Variables Example

# GitHub Personal Access Token with at least: repo, workflow (and any other required) scopes
GH_PAT=

# GitHub username or organization name (e.g. "ContributorUsername" or "my-org")
GITHUB_OWNER=

# GitHub Actions runner registration token (optional; generated by GitHub and expires ~1 hour)
GITHUB_RUNNER_TOKEN=

# Docker Hub access token (or registry token) for pushing/pulling images
DOCKER_ACCESS_TOKEN=

# Docker Hub (or registry) username
DOCKER_USERNAME=ContributorUsername